# Configuration for helm/chart-releaser (cr), used by .github/workflows/release.yaml
# https://github.com/helm/chart-releaser#configuration

# Per-chart file (relative to the chart directory) whose content is used as the
# GitHub release body instead of cr's default auto-generated notes.
release-notes-file: RELEASE_NOTES.md
